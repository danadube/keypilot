/**
 * Thin re-export — presentational layer migrated to PropertyDetailView.
 * page.tsx imports this name; keeping it stable avoids touching routing.
 */
export { PropertyDetailView as PropertyDetail } from "@/components/modules/properties/property-detail-view";
