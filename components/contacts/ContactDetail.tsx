/**
 * Thin re-export — presentational layer migrated to ContactDetailView.
 * page.tsx imports this name; keeping it stable avoids touching routing.
 */
export { ContactDetailView as ContactDetail } from "@/components/modules/contacts/contact-detail-view";
