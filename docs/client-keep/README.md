# ClientKeep — `docs/client-keep/`

**Canonical folder (hyphenated)** for **ClientKeep shipping notes**, internal releases, and playbooks that use the same spelling as routes (`/client-keep/*`) and code paths (`lib/client-keep/`).

## Related docs

- **Architecture / system-of-record** (long-form spec): [`../clientkeep/unified-contact-architecture.md`](../clientkeep/unified-contact-architecture.md) — lives under the legacy **`clientkeep/`** folder name; content is current; only the **directory name** is historical.
- **Platform saved-views spec** (contacts + ShowingHQ patterns): [`../platform/saved-views-spec.md`](../platform/saved-views-spec.md)

When adding new ClientKeep markdown, **prefer this `docs/client-keep/` directory** so links stay consistent with product naming.
