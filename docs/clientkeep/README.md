# ClientKeep — `docs/clientkeep/` (legacy path)

This directory uses the **`clientkeep`** spelling (no hyphen) for **historical path compatibility** only.

- **Prefer** **[`docs/client-keep/`](../client-keep/)** for new ClientKeep operational docs, release notes, and playbooks — it matches route and code naming (`/client-keep`, `lib/client-keep/`).
- **Keep** specs that already live here (e.g. [`unified-contact-architecture.md`](unified-contact-architecture.md)) unless you are doing a deliberate doc-wide move; update **in place** or add a short pointer at the top of a file if you duplicate content elsewhere.
