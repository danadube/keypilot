export default function OpenHouseSignInPage({
  params,
}: {
  params: { id: string };
}) {
  return (
    <div>
      <h1>Sign In</h1>
      {/* TODO: implement */}
      <p>Open House ID: {params.id}</p>
    </div>
  );
}
