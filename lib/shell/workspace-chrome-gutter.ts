/**
 * Workspace chrome wrappers: tab rails removed — {@link DashboardShell} `main` padding
 * is the single horizontal inset. These keep full-width flex behavior for headers + body.
 */
export const KP_WORKSPACE_CHROME_HEADER_GUTTER_CLASS = "w-full min-w-0";

export const KP_WORKSPACE_CHROME_BODY_GUTTER_CLASS = "min-h-0 w-full min-w-0 flex-1";
